console.log("moment: ", moment());
var today = moment().format("dddd");
console.log("today: ", today)


//$(document).ready(function() {
    //I'm using Moment to display the time at the top of the page
    //Information for the date and time on the top of the page. The page needs to update when refreshed
    //var dateTimeDiv = $("<div>")
    //var currentDateTime = moment().format('MMMM Do YYYY')
    
    //dateTimeDiv.html(currentDateTime);
    //$("#currentDay").append(DateTime);
  
  //created local storage setting for scheduler 

  